# Tasks: Refine Battlemap UI Feedback

## 1. Implementation
- [ ] **Refactor SideDock**: Update `SideDock.tsx` to render vertical text labels.
- [ ] **Universal Panels**: Update `BattlemapEditor.tsx` to route `top` and `bottom` closed panels to Side Docks.
- [ ] **Integrated Property Editor**: Integrate `PropertyEditor.tsx` logic into `BattlemapEditor.tsx`'s property panel slot.
- [ ] **Fog Tools**: Move Fog sub-tools back to `BattlemapToolbar.tsx`.
- [ ] **Visual Fixes**: Fix `ColorPicker` z-index and ensure drawing properties are applied.
